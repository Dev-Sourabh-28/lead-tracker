{
  "scripts": {
    "start": "concurrently \"cd server && PORT=5000 npm run start\" \"cd client && PORT=3000 npm start\"",
    "dev": "next dev",
    "build": "next build",
    "lint": "next lint",
    "test": "jest",
    "test:coverage": "jest --coverage"
  }
}